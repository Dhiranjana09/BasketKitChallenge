import XCTest
@testable import BasketKitChallenge

/// Empty on purpose — this is your test target, wired up and ready.
///
/// We want to see your own TDD process from the first test onward: write a
/// failing test for a piece of business logic (stock validation is a good
/// place to start), make it pass with the smallest change that will do it,
/// then keep going. Please don't squash your commits before submitting —
/// the history is part of what we're looking at.
///
/// Test the networking layer against a fake/mock conforming to whatever
/// protocol you design, not against the real npoint URL — a unit test
/// shouldn't depend on the network being up.
final class BasketKitChallengeTests: XCTestCase {
}
